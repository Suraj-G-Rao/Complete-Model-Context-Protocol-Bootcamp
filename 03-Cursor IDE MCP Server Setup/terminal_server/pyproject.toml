[project]
name = "terminal-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Suraj-G-Rao", email = "surajgrao0203@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
terminal-server = "terminal_server:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
